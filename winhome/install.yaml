- defaults:
    shell:
      stdout: true
      stderr: true
      quiet: true

- link:
    ~/winhome:
      force: true
      path: /mnt/c/Users/fbelanger/

- shell:
    - CONFIG=winhome/apps/wezterm/conf.yaml ./Taskfile install
    - CONFIG=winhome/apps/wincompose/conf.yaml ./Taskfile install
    - ./Taskfile app -p winhome wincompose
    - ./Taskfile app -p winhome font
    - ./Taskfile app -p winhome wezterm
    - ./Taskfile app -p winhome win32Yank
