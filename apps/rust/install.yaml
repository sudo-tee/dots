- defaults:
    link:
      create: true
      relink: true
    shell:
      stdout: true
      stderr: true
      quiet: true

- shell:
    - description: "💿💿💿 Rust toolchain 💿💿💿"
      command: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
