- defaults:
    link:
      create: true
      relink: true
    shell:
      stdout: true
      stderr: true
      quiet: true
- link:
    ~/.config/zsh/includes/_fzf.sh:
      force: true
      path: apps/fzf/_fzf.sh
- shell:
    - description: "💫💫💫 Installing fnm 💫💫💫"
      command: |
        if  [ -e ".state/fzf.skip" ]; then
          echo "😎 Skipping fzf install"
        else
          echo "⌛ Installing fnm "
          brew install fzf
          touch .state/fzf.skip
        fi
