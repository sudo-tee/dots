- defaults:
    shell:
      stdout: true
      stderr: true
      quiet: true

- shell:
    - description: "💫💫💫 Installing neovim nightly 💫💫💫"
      command: |
        echo "⌛ Installing neovim nightly"
        brew install --HEAD neovim &&
