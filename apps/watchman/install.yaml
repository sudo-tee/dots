- defaults:
    link:
      create: true
      relink: true
    shell:
      stdout: true
      stderr: true
      quiet: true

- clean: ["~"]
- shell:
    - description: "💿💿💿  Installing watchman 💿💿💿"
      command: |
          brew install watchman
